# Лист → Календарь

Статический прототип для переноса рукописного недельного планера в Google Calendar. Сервер приложения, сборка и секретный ключ не нужны.

## Что работает

- Недельная сетка, листание недель, локальные черновики, изменение и удаление черновика.
- Выбор, перетаскивание или вставка фото из буфера обмена и выделение отдельной строки. Экспериментальное OCR запускается в браузере через [Tesseract.js](https://github.com/naptha/tesseract.js) с русским языковым пакетом. Результат попадает в поле названия и требует ручной правки. Распознавание целой страницы, дат и времени не реализовано. Первый запуск загружает код и языковой пакет; рукопись может распознаваться неточно. OCR на реальном фото пользователя пока не проверен.
- Подключение Google через браузерный OAuth, загрузка событий текущей недели и запись подтверждённого черновика в основной календарь. Изменение и удаление загруженных событий отправляется через Calendar API при активном подключении.
- Простые русские команды: «добавь встречу завтра в 15:00», «перенеси английский на 18:00», «отмени английский». Поиск по названию или курсу может затронуть несколько событий; до выполнения показывается весь список. Это разбор по правилам, не свободный чат.
- Каждое создание, перенос или отмена требует подтверждения. Данные черновиков сохраняются в `localStorage` этого браузера.

## Запуск

В каталоге проекта выполните `python3 -m http.server 8765` и откройте `http://localhost:8765`. Для базового интерфейса достаточно открыть `index.html`; для OAuth нужен HTTP или HTTPS origin, внесённый в настройки Google.

## Google Calendar

Проект Google Cloud и публичный OAuth Client ID уже настроены для `https://paper-calendar.github.io`, `http://localhost:8765` и `http://localhost:8766`. В интерфейсе нажмите «Подключить Google Calendar» и выберите свой Google-аккаунт. У каждого пользователя свой токен и свой основной календарь; токен живёт только в памяти вкладки и исчезает после перезагрузки.

Пока приложение находится в режиме **Testing**, администратор проекта должен добавить каждый Google-адрес в **Google Auth Platform → Audience → Test users**. Иначе Google покажет ошибку 403 `access_denied`.

Секрет клиента не требуется и не должен помещаться в исходники. Используется область доступа `calendar.events`. В демо-режиме без подключения подтверждение превращает черновик в локальное событие, не отправляя его в Google.

## GitHub Pages

Сайт публикуется из ветки `main`, папки `/ (root)`, по адресу [paper-calendar.github.io](https://paper-calendar.github.io/). В репозитории нет личных фотографий, токенов и секретного ключа.

## Приватность и ограничения

Выбранное фото и вырезанный фрагмент обрабатываются в браузере через Canvas; код не отправляет их на сервер. Для OCR браузер загружает код с jsDelivr и русский языковой пакет Tesseract.js. Подтверждённые события и их содержание отправляются в Google Calendar API только при подключённом Google. Черновики остаются в `localStorage` без шифрования: не используйте общий браузер для чувствительного расписания.

OCR предназначено для одной выделенной строки; бледные записи, разные цвета, сжатые фото и рукописные даты могут давать неверный текст. Команды не понимают сложные формулировки, дату переноса или повторяющиеся занятия как серию Calendar recurrence. При изменении нескольких событий запросы к Google идут последовательно; при сетевой ошибке часть уже применённых изменений может сохраниться. Перед подтверждением проверьте список.

## Статус проверки

Проверены синтаксис JavaScript, вставка и перетаскивание тестового изображения, распознавание напечатанной русской строки и разбор команды «добавь встречу завтра в 15:00». Google Calendar API включён; оба аккаунта добавлены в тестовые пользователи. На опубликованном сайте OAuth прошёл до статуса «Google подключён» для `xeningem@gmail.com`. Вход второго аккаунта и OCR на реальном фото не проверены.
